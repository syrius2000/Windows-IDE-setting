# AI Development & Architecture Memo (AI-memo.md)

本ドキュメントは、AI Agent（Google Antigravity / Cursor / Claude等）を用いた開発・保守運用における知見、メトリクス、およびアーキテクチャ上の重要決定事項を記録するメモです。

---

## 🤖 Antigravity セクション

### 1. オフライン対応インタラクティブQuarto解析環境の構築（2026-08-23）

#### 概要と課題
院内・閉域網（オフライン）環境において、臨床医や共同研究者がWebサーバー不要・ダブルクリック（`file://`）起動で閲覧・操作できる対話型解析レポート機能（Quarto）の標準化を実施。

#### 解決した技術的課題
1. **Zero-CDN / Zero-WASM アーキテクチャへの刷新**:
   - Quartoの `{ojs}` / Observable Runtime / DuckDB-WASM 内部による動的外部通信（jsdelivr, unpkg, observableusercontent 等 94〜152件）を完全撤廃。
   - インライン埋め込みJSON（`<script id="offline-cohort-data" type="application/json">`）＋ Pure Local JavaScript ＋ 動的SVGチャート ＋ DOM集計テーブルによる完全自己完結・外部通信ゼロのダッシュボードへ刷新。
2. **期間・治療群・性別の3軸動的フィルタリング**:
   - 観察期間（`<1年 (365日未満)` / `1年以上 (365日以上)`）、治療群（`Active` / `Control`）、性別（`男性` / `女性`）の3軸連動によるリアルタイム再集計・再描画を完備。
3. **厳格な開示統制（小セル全集計値抑制）**:
   - 5例未満（`<5`）のセルは、患者数・イベント数・発生率・平均観察期間（`mean_followup` 含む）の全集計メトリクス列を `None`（`— (<5抑制)`）に自動マスキング。
4. **出力先ディレクトリ統制**:
   - 中間集計JSONを `outputs/private/interactive_cohort_summary.json`、HTMLを `outputs/private/interactive_summary.html` に統一。
   - `reports/quarto/` はソースコード（`.qmd`）のみ管理し、生成成果物の誤コミットを完全防止。
5. **Headless Chrome / CDP による実機ブラウザ自動テスト**:
   - `pytest` スイート内で一時サンドボックス環境（`QUARTO_DATA_DIR` 等）および動的エフェメラルポート・待機ポーリング機構を実装。
   - `file://` 起動、初期描画（タイトル・10行テーブル・8本SVGバー・サマリーカード）、および動的フィルタ操作によるリアルタイム再描画を自動検証（25/25 PASS）。

---

### 2. エージェント開発サイクルと実行メトリクス（実測 ＆ 推定）

OpenSpecワークフロー（`add-offline-interactive-quarto-report`）を通じて実施された開発サイクルの実測値：

| 項目 | 実測値 / 推定値 | 備考 |
|:---|:---:|:---|
| ⏱️ **総所要時間** | **約 30 分** | 03:57 〜 04:28（JST） |
| 💬 **対話ターン数** | **13 ターン** | フィードバック・指示回数 |
| 🤖 **AIアクションステップ数** | **180 ステップ** | 思考（Chain-of-Thought）＋ツール呼び出し |
| 🛠️ **ツール実行総数** | **168 回** | コマンド（77）、ファイル参照（53）、編集（29）等 |
| 🪙 **累積入力トークン（Input）** | **約 4.5 M トークン** | 1ステップあたり平均 25k tokens |
| ✍️ **生成トークン（Output）** | **約 90 k トークン** | 思考・引数・回答文 |
| 🧪 **テスト合格率** | **100% (25/25)** | `pytest -q` |

---

### 3. クォータ・レートリミット（5時間枠・週間枠）の運用知見

AIモデルの利用枠を最大限に活かすための重要メカニズム：

- **二重制限の構造**:
  - **5-hour limit（5時間枠）**: 短時間のバーストアクセスを平滑化し、グローバルリソースを公平分配するための枠（時間経過で順次回復）。
  - **Weekly limit（週間枠）**: ユーザーの契約Tierに紐づく1週間あたりの総利用枠。
- **コスト比例（重み付き）消費**:
  - クォータは会話の「回数」ではなく「トークンの金銭的コスト」に比例して消費される。
  - 軽量・高速なモデル（Flash/Sonnet等）を通常の調査やコード編集に使い、大規模な推論やアーキテクチャ設計に上位モデル（Pro/Opus等）を組み合わせることで、クォータを最も長く維持できる。

---

### 4. 過去会話・プロジェクト累計推定（実測トランスクリプト解析）

Antigravity のローカルログ（`transcript.jsonl`）の実測データに基づく、本プロジェクトにおける主要3セッション（時系列：＃３ → ＃２ → ＃１）の概要と累計メトリクスです。

#### ＃３. Spec-Driven QA レビュー実行（2026-08-22 10:00〜11:31 JST）
- **会話ID**: `abd8c347-98c3-4ac5-b9cb-b94083d38164`
- **所要時間**: 約 1 時間 31 分
- **メトリクス**: ユーザー入力 2回 ｜ AIステップ 66回 ｜ ツール実行 64回 ｜ 推定トークン 約 1.7 M tokens
- **概要**:
  - `spec-driven-qa-review` スキルを活用し、初期仕様（Purpose / Spec / Plan / Tasks）と実装コード・テスト・エビデンスの整合性を網羅的に検証。
  - 要件と実装のドリフト検知、トレーサビリティの確保、およびQAレビュー記録のベースラインを確立。

#### ＃２. Windows 11 RWD/SAS/Cursor環境基盤の設計・Grilling・初期構築（2026-08-22 18:37〜2026-08-23 03:52 JST）
- **会話ID**: `37f535b8-590b-4020-aa6d-1b617d62d711`
- **所要時間**: 約 9 時間 15 分
- **メトリクス**: ユーザー入力 45回 ｜ AIステップ 460回 ｜ ツール実行 415回 ｜ 推定トークン 約 11.7 M tokens
- **概要**:
  - `/grilling` による要件の徹底的なストレステストを実施し、非IT系統計専門家向けの Windows 11 × Cursor × SAS/RWD 開発環境基盤を設計。
  - Copierベースの Case Project 雛形（`templates/analysis-project/`）、Windows環境自動診断・導入PowerShellスクリプト（Strictモード準拠）、SAS任意化・ODBC接続設計、初学者向けREADME・チートシート・Git基本手順、および自動テスト基盤を包括的に構築。

#### ＃１. オフライン対応インタラクティブQuarto報告書＆3軸フィルタ開発（2026-08-23 03:57〜04:28 JST）
- **会話ID**: `084d636d-5bda-45fc-bb3b-4aee3f010069`
- **所要時間**: 約 30 分
- **メトリクス**: ユーザー入力 13回 ｜ AIステップ 180回 ｜ ツール実行 168回 ｜ 推定トークン 約 4.6 M tokens
- **概要**:
  - OpenSpec Change `add-offline-interactive-quarto-report` による完全自己完結型インタラクティブHTML報告書の開発。
  - Zero-CDN/Zero-WASM、3軸フィルタ（観察期間・治療群・性別）、小セル全集計値抑制、Headless Chrome CDP自動テスト（25/25 PASS）、Main Spec同期および変更アーカイブの完了。

---

#### 📊 3セッション合計（プロジェクト累計実測 ＆ 推定）

| 項目 | 3セッション累計値 | 備考 |
|:---|:---:|:---|
| ⏱️ **総実作業時間** | **約 11 時間 16 分** | ＃３(1.5h) ＋ ＃２(9.25h) ＋ ＃１(0.5h) |
| 💬 **総対話ターン数** | **60 ターン** | ユーザーからの指示・レビュー回数 |
| 🤖 **総AIアクションステップ数** | **706 ステップ** | 思考（Chain-of-Thought）＋ツール往復 |
| 🛠️ **総ツール実行回数** | **647 回** | コマンド実行、ファイル精査・編集 |
| 🪙 **累積入力トークン（Input）** | **約 17.65 M トークン** | ステップごとのコンテキスト累積推定 |
| ✍️ **累積生成トークン（Output）** | **約 353 k トークン** | コード生成、回答、ツール引数 |
| 🌐 **総累積トークン量** | **約 18.0 M トークン** | エージェント反復処理の総計算量 |

---

### 関連ドキュメント（相対パス）
- [日常運用マニュアル](docs/daily-operations.md)
- [初心者向けチートシート](docs/beginner-cheatsheet.md)
- [メイン仕様書: interactive-offline-reporting](openspec/specs/interactive-offline-reporting/spec.md)
- [アーカイブされた変更計画](openspec/changes/archive/2026-08-23-add-offline-interactive-quarto-report/proposal.md)

---

## 🧭 Codex セクション（FIX）

### 1. 記録の位置づけ

本セクションは、Antigravityセクションとは別に、Codexとの会話を通じて実施した設計確認、実装結果の再評価、初学者向け文書整備、および最終的なメモリ記録を残すものである。

Antigravityによる既存セクションの更新・上書きが発生しても、Codex側の作業履歴と判断根拠を追跡できるよう、対象範囲、推定メトリクス、成果物、残余リスクを一つのセクションに集約する。

ここに記載する時間・タスク数・トークン量は、API請求情報や内部実行ログから取得した実測値ではない。会話履歴、要求の反復回数、成果物の種類、検証内容から算出したフェルミ推定である。

### 2. Codexが担当した主な作業範囲

#### 2.1 OpenSpec・仕様と実装の確認

- `setup-sas-rwd-ai-environment` の仕様、設計、タスク、実装状況を確認。
- Windows環境自動構築、macOS MySQL/RWDパイプライン、分析プロジェクトガバナンス、プロジェクトファクトリの整合性を確認。
- MySQL 8.0接続について、Python/Rの直接接続を標準経路、ODBCをExcel等向けの任意経路とする方針を確認。
- `add-offline-interactive-quarto-report` のproposal、spec、design、tasks、実装、テスト、アーカイブ状況を確認。

#### 2.2 初学者向けWindows導入の再評価

- クリーンなWindows 11（SAS・Officeのみ）からの導入動線を確認。
- `Setup-Windows.bat`、マスターPowerShell、診断・インストール・設定・検証の順序を確認。
- SASを持たない利用者でもPython/Rで分析を開始できる設計を確認。
- Cursor本体の手動導入とProログイン認証を前提とすることを確認。
- GitHubアカウントなしでも、ローカルGitでプロジェクト作成・作業継続できる導線を確認。

#### 2.3 初学者向け文書の整備・レビュー

- `README.md` の初回セットアップと継続的な統計プロジェクト運用の分離を確認。
- `docs/windows-bootstrap-guide.md`、`docs/beginner-cheatsheet.md`、`docs/daily-operations.md` の役割分担を確認。
- `docs/git-basic-workflow.md` により、GitHub未経験者にもcommit、WIP、squash、push、pullの順序を説明する構成を確認。
- `docs/ai-prompt-recipes.md` により、生存時間解析、データ品質監査、開示統制、臨床家向け要約のプロンプト導線を確認。
- README内の相対リンクが実在することを確認。

#### 2.4 オフラインインタラクティブ報告書の確認

- Quartoで生成する共同研究者向けHTML報告書の目的と利用導線を確認。
- `outputs/private/interactive_cohort_summary.json` と `outputs/private/interactive_summary.html` の私的出力境界を確認。
- Edge/Chromeで開くだけで利用できるローカルHTML、外部CDN・外部通信に依存しない構成を確認。
- 観察期間、治療群、性別によるフィルタリング、小セル抑制、ブラウザ自動検証の要件を確認。
- READMEから日常運用文書へ接続し、生成・検証・releaseへの移行手順を追跡できることを確認。

### 3. Codex会話の推定メトリクス

| 項目 | フェルミ推定 | 算出の考え方 |
|:---|:---:|:---|
| ⏱️ **Codexの総実作業時間** | **約 3〜6 時間** | 仕様確認、文書レビュー、変更方針の整理、検証、記録作成を含む。待機・中断時間は除外。 |
| 💬 **ユーザー指示・レビューラウンド** | **約 30〜40 回** | OpenSpec探索、verify、要求追加、再評価、README確認、メモ更新を含む。 |
| 🧩 **主要タスク群** | **約 12〜16 件** | 機能・成果物・検証単位で集計。OpenSpecのtasks.mdの件数とは別の分類。 |
| 🛠️ **AI作業ステップ** | **約 250〜500 ステップ** | 調査、ファイル参照、差分確認、編集、テスト、検証結果の整理を含む。 |
| 🪙 **累積入力トークン** | **約 0.8〜2.0 M トークン** | 会話履歴、AGENTS.md、OpenSpec、README、関連文書・テストの読み込みを含む概算。 |
| ✍️ **生成・出力トークン** | **約 60〜150 k トークン** | 回答、編集内容、ツール引数、検証報告を含む概算。内部推論の正確な量は含めない。 |
| ✅ **検証・再評価ラウンド** | **約 8〜12 回** | OpenSpec、README導線、Windows初期導入、SAS任意化、ODBC、Quarto報告書を対象に実施。 |

### 4. Codex側で整理した主要な成果

| 分類 | 成果・判断 |
|:---|:---|
| 初期導入 | クリーンなWindows 11から、管理者実行のランチャーを起点に環境構築できる導線を確認。 |
| SAS依存性 | SAS未保有者もPython/Rで開始でき、SASは利用可能な場合のみ追加利用する任意要素として整理。 |
| Cursor | Cursor本体の導入とProログインは事前に手動完了する前提を明確化。 |
| GitHub | GitHubアカウントなしでもローカルGitで開始し、必要になった時点でGitHubを追加できる方針を確認。 |
| MySQL/ODBC | Python/R直接接続を標準、ODBC/DSNをOffice利用者向けの任意プロファイルとする境界を確認。 |
| 初学者文書 | 初期セットアップ、プロジェクト作成、日常運用、Git、AI活用を段階的に分離。 |
| AI活用 | 統計解析、データ品質、開示統制、臨床家向け説明を安全なプロンプト例として整理。 |
| オフライン報告書 | ローカルHTML、動的フィルタ、開示統制、`outputs/private`境界、ブラウザ検証を文書導線に接続。 |
| 仕様管理 | OpenSpecのproposal/spec/design/tasksと実装・テストの対応を確認。 |

### 5. 残余リスクと今後の記録方針

- フェルミ推定値は、Antigravityセクションの実測・推定値と会話境界によって一部重複する可能性がある。
- HTML報告書の最終的な利用可否は、実利用者の端末、Edge/Chromeの環境、院内セキュリティ設定でも確認する必要がある。
- READMEの導線は整備済みだが、初学者による実機での「最初の一回」通し操作を継続的に観察すると改善点を発見できる。
- 将来、数値を実測に近づける場合は、各ラウンドの開始・終了時刻、変更ファイル、テスト件数、ツール実行数、API使用量をラウンド単位で記録する。
- Codexセクションを保護するには、AI Agentに「既存の他エージェント記録を保持し、担当セクションのみ追記・更新する」ことを明示する運用が有効である。

### 6. 記録更新ルール

1. Antigravity、Codex、その他のAI Agentは、それぞれ固有の見出し配下に記録する。
2. 他Agentのセクションを削除・再構成せず、追記または担当範囲内の更新に限定する。
3. 推定値には、必ず「実測」または「フェルミ推定」の区別を付ける。
4. 重要な成果物・テスト・残余リスクは、可能な限りリポジトリ内の相対パスで参照する。
5. 追記前後に`git diff -- docs/ai-memo.md`を確認し、担当セクション以外の意図しない変更がないことを確認する。
